[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "reconnaissance_vul_scan"
version = "0.1.0"
dependencies = []   

[tool.setuptools.packages.find]
where = ["src"]   
include = ["superecon*"]